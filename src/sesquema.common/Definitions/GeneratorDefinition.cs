namespace sesquema.common.Definitions
{
    /// <summary>
    /// Represents a random item.
    /// Granting this item will randomly select one item type from the bundle property, and create an instance of that type.
    /// For example: imagine when a crate is unlocked, then one of the possible items is created.
    /// </summary>
    public class GeneratorDefinition : DefinitionBase
    {
    }
}
